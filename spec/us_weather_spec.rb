# frozen_string_literal: true

require_relative "spec_helper"

describe USWeather do
  it "has a version number" do
    expect(USWeather::VERSION).not_to be nil
  end

  it "can set the HTTP user agent"

  it "can set the HTTP open timeout"

  it "can set the HTTP read timeout"
end
